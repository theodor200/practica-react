const newMessage = (title) => {
     return ( title )
};

export const Variable = ()=>{

    return (
        <>
            <h2>{ newMessage('Titulo') }</h2>
            <p>Soy una variable que viene de ReactJS</p>
        </>
    );

}